#### Create a Separate file `config.js` or any name.
- Import data as per your preference which data you needed.
- Create a class it helps you to not set AppwriteUrl in all function.

```js
import { Client, Databases, ID } from 'appwrite';
import conf from '@/conf/conf';

export class Service{
    client = new Client
    databases

    constructor() {
        this.client
            .setEndpoint(conf.appwriteUrl)
            .setProject(conf.appwriteProjectId)
        this.databases = new Databases(this.client)

    }
}

const service = new Service
export default service
```
==new keyword create a copy of that class==