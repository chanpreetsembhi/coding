- `database.php` file.
```php
<?php

class Database{
      public $connection;
      public function __construct($config, $username = 'root', $password = '')
      
      // http_build_query:- host=localhost&port=3306&dbname=myapp
      $dsn = 'mysql:' . http_build_query($config, '', ';');
      
      $this->connection = new PDO($dsn, $username, $password, [
            PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC
       ]);
       
       public function query($query, $params = []){
            $statement = $this->connection->prepare($query);
            $statement->execute($params);

            return $statement;
    }
}
```

1. `index.php` file.
    -  `function.php` [[06. Functions#Create a Anonymous Function]] ,
    -  `config.php` [[09. MYSQL#Environments and Configuration and cleaner the code]]
```php
require "function.php";
require "database.php";

$config = require ("config.php");

$db = new Database($config['database']);

$id = $_GET['id'];
$query = "select * from posts where id = ?";

$post = $db->query($query, [$id])->fetch();
```
