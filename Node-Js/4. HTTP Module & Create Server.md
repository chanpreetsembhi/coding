#### HTTP Module.

1. Create Create File `server.js` or any Name which you like.
   - `server.js` file structure.
   - `createServer()` want two parameters first is `request` and second one is `response`.
   
```js
import http from 'http';

const PORT = 8000;

const server = http.createServer((req, res) => {

   res.write('Hello World!');
   res.end();
   // res.end('Hello World!');
}

server.listen(8000, () => {
    console.log(`Server running on port ${PORT}`);
});
```
==If you want you can use only **response.end('Hello World')** it works file.==

- HTTP Error request.

```js
import http from 'http';

const PORT = 8000;

const server = http.createServer((req, res) => {
   res.setHeader('Content-Type', 'text/html');
   res.statusCode = 404;
}

server.listen(8000, () => {
    console.log(`Server running on port ${PORT}`);
});
```
==**'text/html'** is file type how they show in browser==

- HTTP Error request compress code.

```js
import http from 'http';

const PORT = 8000;

const server = http.createServer((req, res) => {
    res.writeHead(500, { 'Content-Type': 'application/json'})
    res.end(JSON.stringify({message: 'Server error'}));
}

server.listen(8000, () => {
    console.log(`Server running on port ${PORT}`);
});
```
==**Now:** `res.setHeader()` and `res.statusCode` replace with `res.writeHead()` .==
==In this message show in **JSON** file type.==
 
#### Create Server.

1. If you want that server will automatically restarting change `package.json`  file and one extra file `npm nodemon` [[5. NPM Modules & Nodemon]] install through npm.
   - Add `"start": "node server.js"` is file name. not open terminal and write `npm start` to run the server. 
```json
{
  "name": "js",
  "version": "1.0.0",
  "description": "Node.js crash course code",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "author": "Chanpreet Singh",
  "license": "MIT"
}
```
==**NOTE:** You can write any text in place or start if you write **"dev"** you write
**npm run dev** To start the server.==