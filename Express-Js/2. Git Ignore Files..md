###### If you use `git` software with `github` or `gitlab` so add `.gitignore` file in your project. It help you to ignore that files which you don't want to sent into your repository.

1. Create `.gitignore` File.
   - Never send `node_modules` file into `Github`. because anyone can install this in her machine simple write `npm i` in project directory.

```
node_modules
.env
```
==and add more which you don't push on git.==